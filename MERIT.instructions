# MERIT.instructions (SPRK student-safe)

This file is the public, student-safe adaptation of enterprise MERIT AgenticOps for `SPRK-Hello-Repo`. It does not copy private vault text, operator secrets, or machine-specific paths from the enterprise MERIT instruction set.

## Purpose

- Give students and coding agents one canonical rules file for how this repository is organized.
- Keep classroom missions consistent, testable, and easy to navigate.

## Table of contents

- [Mission folder naming](#mission-folder-naming-required) [[#Mission folder naming (required)]] (obsidian)
- [Mode labels](#mode-labels) [[#Mode labels]] (obsidian)
- [Branch and PR governance](#branch-and-pr-governance-required) [[#Branch and PR governance (required)]] (obsidian)
- [Repository shape](#repository-shape) [[#Repository shape]] (obsidian)
- [Documentation links](#documentation-links-required) [[#Documentation links (required)]] (obsidian)
- [Validation before confidence](#validation-before-confidence) [[#Validation before confidence]] (obsidian)
- [Student-safe MERIT principles](#student-safe-merit-principles) [[#Student-safe MERIT principles]] (obsidian)
- [Related student files](#related-student-files) [[#Related student files]] (obsidian)

## Mission folder naming (required)

Every playable mission under `missions/` must use exactly one folder with this shape:

```text
NN-GameName-<mode-label>
```

| Part | Rule | Example |
| --- | --- | --- |
| `NN` | Two-digit mission number; matches menu, docs, and Playwright registry | `10` |
| `GameName` | PascalCase mission name; no spaces; no internal hyphens | `SpaceInvaders`, `SnakeGame` |
| `<mode-label>` | Player/sync mode: `1P`, `2P`, `nP`, `1P-nP`, or `2P-nP` | `1P-nP` |

Valid examples already in this repo:

- `01-ReactionRace-nP`
- `02-SnakeGame-1P-nP`
- `03-PingPong-2P-nP`
- `10-SpaceInvaders-1P-nP`

Invalid patterns (do not introduce):

- `10-Space-Invaders` (hyphenated game name; missing mode label)
- `missions/10-Space-Invaders` alongside `missions/10-SpaceInvaders-1P-nP` (duplicate mission folders)
- Mission app files in the repository root

Scaffold for the next mission: copy `missions/XX-Template-nP/` and rename to the real `NN-GameName-<mode-label>` before adding gameplay.

When adding a mission, also register it in `tests/helpers/start-mission-server.js`, `tests/helpers/run-playwright.js`, and `package.json` test scripts.

## Mode labels

| Label | Meaning |
| --- | --- |
| `1P` | One player |
| `2P` | Two players |
| `nP` | Many players (classroom) |
| `1P-nP` | Starts single-player; can grow to classroom multiplayer |
| `2P-nP` | Starts two-player; can grow to classroom multiplayer |

The folder suffix must match the Mode column in `README.md` Mission Menu.

## Branch and PR governance (required)

One task equals one named branch and one pull request. Do not create a second branch or PR for the same task unless the first branch/PR is explicitly abandoned and the reason is recorded in the replacement PR.

Rules:

- Start every task by identifying the canonical task branch and PR.
- Put follow-up fixes, conflict resolution, validation refreshes, and review changes for that task on the same branch and PR.
- If a task needs multiple review checkpoints, track them in the same PR body or checklist; do not create parallel branches for the same intent.
- If someone asks to track multiple PRs for one task, treat them as views/checkpoints of the same canonical branch unless they explicitly approve separate scoped branches.
- If duplicate PRs already exist, choose one canonical PR, port any missing useful changes into it, and close the duplicates.
- Separate branches are allowed only for separate tasks, separate base branches, or explicitly abandoned/restarted work.

This rule prevents duplicate mission folders, split validation evidence, and branch tentacles that make `main` hard to reason about.

## Repository shape

- Root: workspace files only (`README.md`, `package.json`, `docs/`, `tests/`, instruction files).
- `missions/<NN-GameName-mode>/`: each mission’s `index.html`, `server.py`, `src/`, and `docs/`.
- `missions/_shared/`: shared backend/frontend helpers and generated baseline status.
- `tests/`: Playwright specs and artifacts (not committed when generated during local runs).

Do not rename `docs/` to a MERIT `{Name} docs/` folder in this student repo; curriculum links depend on `docs/`.

## Documentation links (required)

SPRK docs must read correctly in **GitHub**, **Codespaces**, **VS Code Markdown preview**, and optional **Obsidian**. Use two link forms when pointing at a **section** inside a document.

### Portable link (everywhere)

Use standard Markdown relative paths and GitHub-style heading anchors. This is the link students click in the browser and in the editor preview.

```markdown
[Visible label](path/to/doc.md#heading-anchor)
```

Rules:

- Paths are **relative to the file you are editing** (for example `../MERIT.instructions` from `docs/`, or `docs/SPRK_Facilitator_Guide.md` from the repo root).
- Heading anchors use lowercase words separated by hyphens (GitHub slug rules).
- Use portable links in tables, mission guides, issues, and agent handoffs.

### Obsidian link (section references)

When the target is a **section** (not just a whole file), add an Obsidian wikilink immediately after the portable link and label it **`(obsidian)`**:

```markdown
[Validation before confidence](MERIT.instructions#validation-before-confidence) [[MERIT.instructions#Validation before confidence]] (obsidian)
```

Rules:

- `NoteName` is the Markdown filename **without** `.md`.
- `Exact Heading Text` matches the target heading line (Obsidian uses the heading text after `#`).
- Same-file section jump: `[Mission naming](#mission-folder-naming-required) [[#Mission folder naming (required)]] (obsidian)`
- Whole-file targets need only the portable link; Obsidian wikilink is optional.
- Do **not** use `(obsidian)` for external URLs. Do **not** paste private MERIT vault `obsidian://` URIs into this repo.

### When to use each

| Target | Portable link | `[[wikilink]] (obsidian)` |
| --- | --- | --- |
| Section in this file | Required | Required |
| Section in another file | Required | Required |
| Whole file | Required | Optional |
| External site (GitHub.com, obsidian.md) | Required | Do not add |

### Obsidian vault setup (optional)

Open the **repository root** (the folder that contains `README.md`, `MERIT.instructions`, and `docs/`) as the vault so wikilinks like `[[docs/SPRK_Facilitator_Guide#Start Here]]` resolve. Obsidian is optional; the portable link is always enough for classroom work.

Enterprise MERIT L1/L2/L3 instructions remain in the **private MERIT Obsidian vault** (separate from this repo).

### Documentation index

- [docs/README.md](docs/README.md) — master table of contents for repo-wide guides.

## Validation before confidence

From the repository root:

```bash
npm run setup:missions
npm run validate
```

Mission-specific quick check:

```bash
npm run test:spaceinvaders
```

A mission change is not ready until the relevant Playwright baseline passes and only intended files are staged.

## Student-safe MERIT principles

1. Single source of truth — link to an existing guide instead of duplicating long explanations.
2. Validation before confidence — Playwright baseline and Baseline Status tab reflect real backends.
3. Secrets stay local — use `.env.local` if needed; commit only `.env.example` templates.
4. No hard-coded machine paths — use repo-relative paths and helpers.
5. Recovery over mystery — errors and docs should say what to try next.

## Related student files

- [docs/README.md](docs/README.md) — documentation hub (portable links + section wikilinks per [Documentation links](#documentation-links-required))
- [AGENTS.md](AGENTS.md) — agent read order and closeout checklist
- [AgentDraven.instructions](AgentDraven.instructions) — classroom mentor tone
- [docs/SPRK_Student_AgenticOps_Guide.md](docs/SPRK_Student_AgenticOps_Guide.md) — MERIT enterprise vs SPRK student compare/contrast
- [docs/PROJECT_GUIDE.md](docs/PROJECT_GUIDE.md) — local formative insights and maintainer pointers
- [ops/README.md](ops/README.md) — hygiene and handoff format

## What not to import from enterprise MERIT

- Private vault paths or full enterprise instruction bodies
- Operator-only billing, tenant, or admin-console requirements for beginner missions
- Duplicate mission folders or alternate naming schemes for the same game
